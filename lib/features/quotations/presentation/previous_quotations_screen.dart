import 'package:flutter/material.dart';

class PreviousQuotationsScreen extends StatelessWidget {
  const PreviousQuotationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Previous Quotations')),
      body: const Center(child: Text('Previous Quotations List Placeholder')),
    );
  }
}
